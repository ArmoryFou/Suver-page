<!DOCTYPE html>
<html>
  <head>

    <meta property="og:site_name" content="Suver"/>
    <meta property="og:title" content="Suver Page"/>
    <meta property="og:description" content="La página oficial de Suver Server"/>
    <meta property="og:image" content="https://" + process.env.HEROKU_APP_NAME + ".herokuapp.com\/images/background.jpg">
    <meta property="og:image:type" content="image/jpg">
    <meta property="og:image:width" content="250">
    <meta property="og:image:height" content="250">
    <meta name="theme-color" content="#FFFFFF">
    <meta property="og:url" content="https://" + process.env.HEROKU_APP_NAME + ".herokuapp.com\/"/>
    <meta property="og:type" content="server"/>
    <meta name="theme-color" content="#c12d7a"/>

    <script src="javascripts/llop.js"></script>

    <!-- CSS PC -->

    <link rel="icon" href="images/suveradaicon.ico" />
    <link
      rel="stylesheet"
      media="only screen and (min-width: 1000px)"
      href="/stylesheets/pc/general.css"
    />
    <link
      rel="stylesheet"
      media="only screen and (min-width: 1000px)"
      href="/stylesheets/pc/profile.css"
    />
    <link
      rel="stylesheet"
      media="only screen and (min-width: 1000px)"
      href="/stylesheets/pc/navbar.css"
    />

    <!-- CSS MOVIL -->

    <link
      rel="stylesheet"
      media="only screen and (max-width: 1000px)"
      href="stylesheets/movil/movil.css"
    />

    <!-- BOOTSTRAP -->

    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3"
      crossorigin="anonymous"
    />

    <!-- FONTS -->

    <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@500&display=swap" rel="stylesheet">

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Anonymous+Pro:ital@1&display=swap"
      rel="stylesheet"
    />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Armata&display=swap"
      rel="stylesheet"
    />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  </head>

  <body>

    <div class="container">
      <div class="profile-header">
        <img src="<%=ppimage%>" alt="" class="profile-image">
        <h2 class="profile-username"><%= username %></h2>
        <% if(edit) {%>
          <a class="btn edit-profile-btn" href="/profiles/<%=id%>/edit" role="button">Edit Profile</a>
        <% } %>
      </div>
      <form method="POST" action="/posts/create" class="create-post-form">
        <textarea name="postText" placeholder="What's on your mind?" rows="3" required></textarea>
        <button type="submit" class="btn post-btn">Post</button>
      </form>
      <div class="posts-container">
        <h2>My Posts</h2>
        <% if (posts.length === 0) { %>
          <p>No posts yet!</p>
        <% } else { %>
          
          <% posts.reverse();
           for (let i = 0; i < posts.length; i++) { %>
            <div class="post">
              <div class="post-header">
                <img src="<%= posts[i].pp %>" alt="" class="post-profile-image">
                <h3 class="post-username"><%= posts[i].username %></h3>
              </div>
              <p class="post-text"><%= posts[i].postText %></p>
            </div>
          <% } %>
        <% } %>
      </div>
    </div>    
  
    <% if(typeof alert != "undefined") { %>
      <script src="//cdn.jsdelivr.net/npm/sweetalert2@11"></script>
      <script>
        window.onload = function() {
          Swal.fire({
            title: "<%= alertTitle %>",
            text: "<%= alertMessage %>",
            icon: "<%= alertIcon %>",
            showConfirmButton: <%= showConfirmButton %>,
            timer: <%= timer %>
          }).then(()=>{
            window.location= "/<%= ruta %>"
          })
        }
      </script>
    <% } %>
  
  </body>
</html>